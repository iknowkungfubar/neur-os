[project]
name = "neur-os"
version = "0.4.0"
description = "NeurOS — ambient wellness operating system"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.5.0",
    "httpx>=0.27.0",
]

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.ruff.lint.per-file-ignores]
"backend/test_*.py" = ["E402", "E501"]
"backend/store/sqlite_store.py" = ["E501"]
"backend/store/memory_store.py" = ["E501"]
"backend/domain/*.py" = ["E501"]
"backend/store/base.py" = ["E501"]
"backend/routes/declarative.py" = ["E501"]
"backend/routes/tasks.py" = ["E501"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["backend"]
python_files = ["test_*.py"]
asyncio_mode = "auto"

[tool.coverage.report]
fail_under = 60
show_missing = true
